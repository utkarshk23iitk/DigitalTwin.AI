# Optional: enables the cloud explanation layer in the AI Copilot workspace.
# All plant metrics and predictions work without this variable.
GEMINI_API_KEY=replace_with_your_server_side_key
GEMINI_MODEL=gemini-2.5-flash
